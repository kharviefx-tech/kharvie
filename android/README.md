# Android store target

This directory is the reserved native Android target for the Kharvie Capacitor app.

Generate the native project once with:

`npx cap add android`

Then sync web assets with:

`npx cap sync android`

For Google Play, build a signed Android App Bundle (`.aab`) and target API 36 or higher for a new app submitted after August 31, 2026.
